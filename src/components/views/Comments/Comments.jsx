import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

function chat(){
  
}

const Comments = () => (
  <div onClick = {chat} className = 'status-comment'>

  </div>
);

// Status.propTypes = {
//   name: PropTypes.string.isRequired,
// };

export default Comments;
